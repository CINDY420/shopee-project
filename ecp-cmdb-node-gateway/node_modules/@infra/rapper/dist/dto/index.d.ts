import { Intf } from '../types';
import { format } from 'json-schema-to-typescript/dist/src/formatter';
import { DEFAULT_OPTIONS } from 'json-schema-to-typescript';
declare type ConfigType = {
    interfaces: Array<Intf>;
    outputFiles: {
        path: string;
        content: string;
    }[];
    format: typeof format;
    options: typeof DEFAULT_OPTIONS;
    rapperPath: string;
    rapUrl: string;
    projectId: number;
    rapperVersion: string;
};
/** generate ts */
declare function createDtoFiles(config: ConfigType): void;
export default createDtoFiles;
