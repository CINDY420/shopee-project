export enum USER_TYPE {
  HUMAN = 'human',
  BOT = 'bot',
}

export enum QUOTA {
  CPU = 'cpu',
  MEMORY = 'memory',
  GPU = 'gpu',
}

export enum USER_MODAL_TITLE {
  CREATE_MODAL = 'Add User',
  EDIT_MODAL = 'Edit User',
}
