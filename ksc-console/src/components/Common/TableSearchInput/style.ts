import styled from 'styled-components'
import { Row, Input } from 'infrad'

export const StyledRow = styled(Row)`
  padding: 8px 0 24px 0;
`

export const StyledInput = styled(Input)`
  width: 320px;
`
