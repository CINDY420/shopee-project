import styled from 'styled-components'
import { Row, Button } from 'infrad'

export const StyledRow = styled(Row)`
  padding: 8px 0 24px 0;
`

export const StyledButton = styled(Button)`
  width: 122px;
`
