import styled from 'styled-components'
import { Layout } from 'infrad'

const { Content } = Layout

export const CommonStyledContent = styled(Content)`
  width: 100%;
  height: 100%;
  padding: 0 24px;
  overflow: hidden;
  /* border: solid 1px; */
`
