// DO NOT MODIFY THIS FILE
// THIS FILE IS AUTOGENERATED
import {
  PageAlias,
} from './types'

import {
  ECP_CMDB_PAGE_URLPATH,
} from './constants'

interface PathAliasAs<T> {
  _alias: T
}

export function getPageUrl(alias: PageAlias.EcpCmdbPage): string
export function getPageUrl(alias: PageAlias | PathAliasAs<PageAlias>): string

export function getPageUrl(alias: PageAlias | PathAliasAs<PageAlias>): string {
  // if an object, treat alias as a PathAliasAs<PageAlias>
  if (typeof alias === 'object') {
    if ('_alias' in alias) {
      return getPageUrl(alias._alias as PageAlias)
    } else {
      throw new Error("getPageUrl(): 'alias' is an object, but does not have 'alias._alias'")
    }
  }

  // otherwise, treat alias as a PageAlias
  switch (alias) {
    case PageAlias.EcpCmdbPage:
      return ECP_CMDB_PAGE_URLPATH
    default:
      throw new Error(`getPageUrl(): Unknown page alias ${alias}.`)
  }
}
