import styled from 'styled-components'
import { Typography } from 'infrad'
const { Text } = Typography

export const HPAControllerWrapper = styled.div`
  height: 64px;
  background-color: #fafafa;
  display: flex;
  align-items: center;
`
export const HPAControllerText = styled(Text)`
  margin-left: 16px;
  margin-right: 8px;
  font-size: 14px;
`
