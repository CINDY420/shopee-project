import { Breadcrumb } from 'infrad'
import styled from 'styled-components'

export const StyledBreadcrumb: any = styled(Breadcrumb)`
  display: flex;

  & > span {
    display: flex !important;
  }
`
